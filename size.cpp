#include <iostream>
#include <vector>

int main() {
    std::vector<int> arr = {1, 2, 3, 4, 5};

    // Using size() method to get the number of elements in the vector
    std::cout << "Number of elements in the vector: " << arr.size() << std::endl;

    return 0;
}
